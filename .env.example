# ===========================================
# KIDzAPP - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ===========================================
# REQUIRED - Database
# ===========================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/kidzapp

# ===========================================
# REQUIRED - Security
# ===========================================
# Session secret for Express sessions (generate a random 32+ character string)
# Example: openssl rand -hex 32
SESSION_SECRET=your-super-secret-session-key-change-this

# Encryption key for sensitive data (AES-256-GCM)
# Must be exactly 32 characters for AES-256
# Example: openssl rand -hex 16
WALLET_ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# OPTIONAL - Web Push Notifications
# ===========================================
# Generate VAPID keys with: npx web-push generate-vapid-keys
# If not provided, push notifications will be disabled
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# ===========================================
# OPTIONAL - Server Configuration
# ===========================================
# Port to run the server on (default: 5000)
PORT=5000

# Node environment (development or production)
NODE_ENV=production

# ===========================================
# NOTES
# ===========================================
# Bitcoin/Lightning Configuration:
# - LNBits and NWC credentials are configured per-user in the app
# - No global Lightning configuration needed
#
# Database:
# - Supports standard PostgreSQL and Neon serverless
# - Run migrations: npm run db:push
#
# Security:
# - Never commit this file with real values!
# - All secrets should be unique and randomly generated
